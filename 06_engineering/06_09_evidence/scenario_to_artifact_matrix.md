# Scenario To Artifact Matrix

| Scenario | Purpose | Command / Run Path | Expected Result | Actual Result | Artifact Location(s) | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Command truth-loop | Prove that a valid operator command traverses the full software chain and produces accepted state/evidence updates. | `python3 06_engineering/06_06_integration/integration_runner.py` | Mode command accepted, manual command accepted, telemetry and status observed, backend stores evidence. | `summary: passed=10 total=10`; mode command accepted; manual telemetry found; final state reached `SAFE_STOP` after later heartbeat timeout. | `artifacts/integration_full_chain.log` | PASS | This is the strongest single end-to-end software artifact. |
| Invalid input rejection | Prove unsafe or unsupported input is rejected rather than silently accepted. | `python3 06_engineering/06_06_integration/integration_runner.py` | Unsupported mode request yields rejection audit evidence. | `invalid_cmd_found=True invalid_audit_found=True`. | `artifacts/integration_full_chain.log` | PASS | Rejection reason is visible in the event/audit flow. |
| Degraded -> safe-stop behavior | Prove link degradation escalates into safe-stop behavior within the software contour. | `python3 06_engineering/06_06_integration/integration_runner.py` | Heartbeat loss triggers degraded state, then safe-stop, with alarm and heartbeat evidence. | `final_state=SAFE_STOP degraded_alarm=True degraded_heartbeat=True safe_stop_alarm=True`. | `artifacts/integration_full_chain.log` | PASS | Software-only logic proof, not real safety validation on hardware. |
| Full-chain integration path | Prove the canonical local contour works from operator through backend and MQTT to edge and back. | `python3 06_engineering/06_06_integration/integration_runner.py` | Full chain passes all scenario checks. | `summary: passed=10 total=10`. | `artifacts/integration_full_chain.log` | PASS | Canonical non-UI evidence path. |
| UI operator/manual-QA path | Prove the browser-facing path can show status, receive live updates, and send commands through existing backend/API path. | `python3 06_engineering/06_08_ui/ui_demo_stack.py`; `npm run dev -- --host 127.0.0.1 --port 5173`; `node ui_smoke.mjs` | UI HTTP responds; backend health is OK; mode and manual commands publish; live frames and telemetry appear. | UI returned `200 OK`; backend returned `status=ok`; smoke summary shows `mode_published=true`, `manual_published=true`, `live_frames=9`, `telemetry_seen=1`, final state `MANUAL`. | `artifacts/ui_http_head.txt`; `artifacts/backend_health.json`; `artifacts/ui_smoke_summary.json` | PASS | No screenshot artifact was collected in this sprint. |
| Frozen software-only MVP boundary | Prove the demo package and truth layer explicitly limit claims to the software-only contour. | `python3 06_engineering/06_07_polish/demo_runner.py`; document review | Demo final summary and top-level truth docs limit claims to software-only MVP. | `software_mvp_frozen=true`; `hardware_specific_code_introduced=false`; `claims_limited_to=software_only_contour`. | `artifacts/polished_demo_summary.log`; `../../TOP_LEVEL_TRUTH_BASELINE.md`; `../06_07_polish/mvp_freeze_manifest.md` | PASS | This is scope-boundary evidence, not runtime-performance evidence. |
| Backend ingest / serving path | Prove backend ingests MQTT messages, exposes current status, and serves live/web data. | `python3 06_engineering/06_04_backend/backend_demo_runner.py` | WebSocket handshake succeeds; command/event/status/telemetry ingested; health endpoint stays OK. | WebSocket connected; one command/event/telemetry snapshot stored; health stayed OK with `storage_mode=dev-memory`. | `artifacts/backend_demo_ingest.log`; `artifacts/backend_health.json` | PASS | This proves dev/demo storage path, not PostgreSQL runtime. |
