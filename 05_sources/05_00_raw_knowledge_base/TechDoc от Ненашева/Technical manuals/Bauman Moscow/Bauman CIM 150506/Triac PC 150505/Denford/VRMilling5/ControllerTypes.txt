CONTROLLER_NEWCOMMS = 0;         // based on old thread with a different comm port driver

CONTROLLER_NEXTMOVE = 1;
  
CONTROLLER_CONECT = 2;           // connect router

CONTROLLER_TRIO = 3;

CONTROLLER_NEW = 4;              // first test of new Denstep-crappy cos of using timer

CONTROLLER_DENSTEP = 5;          // original lowlevel and comdriver32 - no good on WinNt- hangs up on teddy

CONTROLLER_NEWCOMMSNEWCHIP = 6;  // based on old thread with a different comm port driver and better checksum check

CONTROLLER_NEXTMOVEES = 7;      // the new Denstep top board from baldor (NextStep 5035)

CONTROLLER_NEXTSTEP = 8;        // the new baldor Denstep ver 5114 OCX

CONTROLLER_NEXTSTEP52XX = 9;    // 5204 OCX version with USB support

CONTROLLER_NEXTMOVEESB = 10;    // based on NextStep52XX but its a NextMove ESB (servo controller)


NextStepBaudRates: array[0..3] of integer = (57600, 115200, 19200, 38400);
